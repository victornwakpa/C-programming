#include <stdio.h>

int main() {
    printf("My first C code \n"); // "\n" this is used to introduce a new line
    printf("Another line of code!");
    return 0; // the return statement returns 1 if error, then 0 if no error
}